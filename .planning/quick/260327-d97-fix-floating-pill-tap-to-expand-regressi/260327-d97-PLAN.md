---
phase: quick-260327-d97
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ZenSocial/Views/FloatingPillButton.swift
autonomous: true
requirements: [QUICK-FIX-PILL-TAP]
must_haves:
  truths:
    - "Collapsed floating pill still expands on tap after the drag tracking rewrite"
    - "Real-time drag tracking remains intact"
    - "Pan failure/cancel paths do not reset the pill position when no drag began"
  artifacts:
    - path: "ZenSocial/Views/FloatingPillButton.swift"
      provides: "UIKit gesture bridge that supports both tap-to-expand and drag"
      contains: "UITapGestureRecognizer"
  key_links:
    - from: "FloatingPillButton.collapsedPill"
      to: "PillDragView.onTap"
      via: "overlay recognizers route tap and drag separately"
      pattern: "tap.require\\(toFail: pan\\)"
---

<objective>
Fix the floating pill regression introduced by the UIKit drag bridge so a normal tap still expands the pill while drag continues to follow the finger in real time.

Output: Updated FloatingPillButton.swift with distinct tap and drag handling.
</objective>

<execution_context>
@.planning/STATE.md
@ZenSocial/Views/FloatingPillButton.swift
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Restore tap-to-expand while preserving drag tracking</name>
  <files>ZenSocial/Views/FloatingPillButton.swift</files>
  <action>
Update the UIKit gesture bridge in FloatingPillButton.swift so the collapsed pill supports both interaction paths:

1. Add a dedicated UITapGestureRecognizer to PillDragView and wire it to the existing onTap callback.
2. Require the tap recognizer to fail when the pan recognizer succeeds so drags still own drag interactions.
3. Guard the pan lifecycle so cancelled/failed recognitions do not reset pillPosition unless a drag actually started.

Do not change the expanded pill buttons or navigation behavior.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/ZenSocial && xcodebuild -scheme ZenSocial -destination 'platform=iOS Simulator,name=iPhone 17' build</automated>
  </verify>
  <done>
    - Collapsed pill expands on tap again
    - Dragging still updates position in real time
    - Failed taps do not move the pill to an incorrect location
    - Project compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restored tap recognition in the floating pill's UIKit gesture bridge while keeping drag behavior intact.</what-built>
  <how-to-verify>
    1. Run the app in Simulator
    2. Tap the collapsed floating pill and verify it expands
    3. Drag the collapsed pill and verify it follows your finger with no lag
    4. Release near edges and verify it still clamps correctly
    5. Tap an item in the expanded pill and verify navigation still works
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- The collapsed pill uses a UITapGestureRecognizer path for taps
- Pan cancellation/failure no longer resets position when no drag is active
- Project builds without errors
</verification>

<success_criteria>
- Tap-to-expand works again
- Drag-follow behavior remains smooth
- No new regression in pill position or expanded navigation behavior
</success_criteria>

<output>
After completion, create `.planning/quick/260327-d97-fix-floating-pill-tap-to-expand-regressi/260327-d97-SUMMARY.md`
</output>
