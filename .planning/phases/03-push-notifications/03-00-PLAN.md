---
phase: 03-push-notifications
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - ZenSocialTests/NotificationPermissionTests.swift
  - ZenSocialTests/APNsRegistrationTests.swift
  - ZenSocial.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - PUSH-01
  - PUSH-02

must_haves:
  truths:
    - "xcodebuild test -scheme ZenSocial runs and finds ZenSocialTests target"
    - "NotificationPermissionTests.swift contains stub tests for permission request flow"
    - "APNsRegistrationTests.swift contains stub tests for APNs token registration"
  artifacts:
    - path: "ZenSocialTests/NotificationPermissionTests.swift"
      provides: "XCTest stubs for PUSH-01 permission and foreground notification behavior"
      contains: "XCTestCase"
    - path: "ZenSocialTests/APNsRegistrationTests.swift"
      provides: "XCTest stubs for PUSH-02 APNs token registration behavior"
      contains: "XCTestCase"
  key_links:
    - from: "ZenSocial.xcodeproj"
      to: "ZenSocialTests/"
      via: "Test target in Xcode project"
      pattern: "ZenSocialTests"
---

<objective>
Create the XCTest infrastructure for Phase 3: a ZenSocialTests test target in the Xcode project and stub test files for notification permission and APNs registration. This is Wave 0 -- all subsequent plans depend on these test stubs existing so their verify commands can run xcodebuild test.

Purpose: Nyquist compliance requires automated test verification for every task. No test target currently exists in the project. This plan creates the target and stubs so Plans 01 and 02 can verify against real test runs.

Output: ZenSocialTests target in Xcode project, two test stub files with failing/placeholder tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-push-notifications/03-CONTEXT.md
@.planning/phases/03-push-notifications/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ZenSocialTests target and test stub files</name>
  <files>
    ZenSocialTests/NotificationPermissionTests.swift,
    ZenSocialTests/APNsRegistrationTests.swift,
    ZenSocial.xcodeproj/project.pbxproj
  </files>
  <read_first>
    ZenSocial.xcodeproj/project.pbxproj (understand existing target structure, build settings)
  </read_first>
  <action>
    **1. Create `ZenSocialTests/` directory** at project root.

    **2. Create `ZenSocialTests/NotificationPermissionTests.swift`:**
    ```swift
    import XCTest
    @testable import ZenSocial

    final class NotificationPermissionTests: XCTestCase {

        func testNotificationManagerExists() {
            // Stub: verify NotificationManager singleton is accessible
            // Will be implemented when NotificationManager is created in Plan 01
            XCTFail("Stub — NotificationManager not yet implemented")
        }

        func testPermissionRequestFlowTriggersAfterLogin() {
            // Stub: verify triggerPermissionFlowIfNeeded sets showPrePrompt
            // when hasRequestedPermission == false and permissionStatus == .notDetermined
            XCTFail("Stub — permission flow not yet implemented")
        }

        func testForegroundNotificationRetursBannerOptions() {
            // Stub: verify AppDelegate willPresent returns [.banner, .sound, .badge]
            XCTFail("Stub — foreground handling not yet implemented")
        }
    }
    ```

    **3. Create `ZenSocialTests/APNsRegistrationTests.swift`:**
    ```swift
    import XCTest
    @testable import ZenSocial

    final class APNsRegistrationTests: XCTestCase {

        func testAPNsTokenManagerExists() {
            // Stub: verify APNsTokenManager singleton is accessible
            XCTFail("Stub — APNsTokenManager not yet implemented")
        }

        func testTokenRegistrationConvertsDataToHex() {
            // Stub: verify registerToken converts Data to hex string
            XCTFail("Stub — token registration not yet implemented")
        }

        func testTokenPersistsAcrossAccess() {
            // Stub: verify stored token is retrievable via getToken()
            XCTFail("Stub — token persistence not yet implemented")
        }
    }
    ```

    **4. Add ZenSocialTests target to Xcode project:**
    - Add a new unit test target `ZenSocialTests` to `ZenSocial.xcodeproj/project.pbxproj`.
    - The test target must depend on the main `ZenSocial` app target (TEST_HOST = the built app).
    - Add both .swift files to the test target's source build phase.
    - Set PRODUCT_BUNDLE_IDENTIFIER to `com.zensocial.ZenSocialTests` (or match the main app's org prefix).
    - Set SWIFT_VERSION = 6.0, deployment target iOS 17.0 to match main target.
    - IMPORTANT: Modifying pbxproj manually is fragile. Prefer using `xcodegen` if a project.yml exists, or carefully edit the pbxproj following the existing target structure as a template. Alternatively, use a `swift package` test target if the project supports it. The key requirement is that `xcodebuild test -scheme ZenSocial -destination 'platform=iOS Simulator,name=iPhone 16'` discovers and runs the tests.

    **5. Verify the test target is recognized:**
    Run `xcodebuild test -scheme ZenSocial -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ZenSocialTests 2>&1 | tail -20` and confirm tests are discovered (they will fail, which is expected for stubs).
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/ZenSocial && xcodebuild test -scheme ZenSocial -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ZenSocialTests 2>&1 | grep -E "Test Suite|Executed|FAIL" | tail -10</automated>
  </verify>
  <done>ZenSocialTests target exists in Xcode project, both test stub files compile, xcodebuild test discovers 6 stub tests (all expected to fail with "Stub" messages). Wave 0 is complete -- Plans 01 and 02 can now use xcodebuild test for verification.</done>
</task>

</tasks>

<verification>
1. `xcodebuild test -scheme ZenSocial -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ZenSocialTests` discovers tests (exit may be non-zero due to XCTFail stubs, but tests are found)
2. `ZenSocialTests/NotificationPermissionTests.swift` exists with 3 stub test methods
3. `ZenSocialTests/APNsRegistrationTests.swift` exists with 3 stub test methods
</verification>

<success_criteria>
- xcodebuild test finds and attempts to run ZenSocialTests target
- Both test files are compiled as part of the test target
- Stub tests fail with descriptive "Stub" messages (red phase of TDD)
</success_criteria>

<output>
After completion, create `.planning/phases/03-push-notifications/03-00-SUMMARY.md`
</output>
