---
phase: 03-push-notifications
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - ZenSocialTests/NotificationManagerTests.swift
  - ZenSocialTests/APNsTokenTests.swift
  - ZenSocialTests/NotificationPayloadTests.swift
  - ZenSocial.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - PUSH-01
  - PUSH-02

must_haves:
  truths:
    - "XCTest target ZenSocialTests exists in the Xcode project"
    - "Test stubs compile and can be run via xcodebuild test"
  artifacts:
    - path: "ZenSocialTests/NotificationManagerTests.swift"
      provides: "Unit test stubs for NotificationManager permission flow logic"
      contains: "class NotificationManagerTests"
    - path: "ZenSocialTests/APNsTokenTests.swift"
      provides: "Unit test stubs for APNs device token formatting"
      contains: "class APNsTokenTests"
    - path: "ZenSocialTests/NotificationPayloadTests.swift"
      provides: "Unit test stubs for notification payload deep-link URL extraction"
      contains: "class NotificationPayloadTests"
  key_links: []
---

<objective>
Create the XCTest target and test stub files for Phase 3 notification logic. These stubs define the test contracts that subsequent plans implement against.

Purpose: Satisfies the Nyquist rule -- automated test files must exist before production code. Provides test scaffolding for NotificationManager permission flow, APNs token formatting, and notification payload parsing.
Output: ZenSocialTests/ directory with three test stub files and the XCTest target registered in the Xcode project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-push-notifications/03-CONTEXT.md
@.planning/phases/03-push-notifications/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XCTest target and notification test stubs</name>
  <files>
    ZenSocialTests/NotificationManagerTests.swift,
    ZenSocialTests/APNsTokenTests.swift,
    ZenSocialTests/NotificationPayloadTests.swift,
    ZenSocial.xcodeproj/project.pbxproj
  </files>
  <read_first>
    ZenSocial.xcodeproj/project.pbxproj,
    .planning/phases/03-push-notifications/03-RESEARCH.md
  </read_first>
  <action>
    **1. Create ZenSocialTests directory and XCTest target:**

    If no ZenSocialTests target exists in the Xcode project, add one. Create the `ZenSocialTests/` directory and register it as a unit test target in `project.pbxproj` linked to the ZenSocial app target.

    **2. Create `ZenSocialTests/NotificationManagerTests.swift`:**

    Test stubs for NotificationManager permission flow logic:

    ```swift
    import XCTest
    @testable import ZenSocial

    final class NotificationManagerTests: XCTestCase {

        // MARK: - Permission Flow

        func testShouldShowPrePromptWhenNotDeterminedAndNotYetShown() {
            // Stub: Verify shouldShowPrePrompt returns true when
            // authorizationStatus == .notDetermined and prePromptShown == false
            XCTFail("Not yet implemented")
        }

        func testShouldNotShowPrePromptAfterMarkedShown() {
            // Stub: After markPrePromptShown(), shouldShowPrePrompt returns false
            XCTFail("Not yet implemented")
        }

        func testShouldNotShowPrePromptWhenAlreadyAuthorized() {
            // Stub: When authorizationStatus == .authorized, shouldShowPrePrompt returns false
            XCTFail("Not yet implemented")
        }

        // MARK: - User Preferences

        func testUserWantsNotificationsDefaultsToTrue() {
            // Stub: Default value of userWantsNotifications is true
            XCTFail("Not yet implemented")
        }
    }
    ```

    **3. Create `ZenSocialTests/APNsTokenTests.swift`:**

    Test stubs for APNs device token formatting:

    ```swift
    import XCTest
    @testable import ZenSocial

    final class APNsTokenTests: XCTestCase {

        func testDeviceTokenFormattedAsHexString() {
            // Stub: Data bytes [0xDE, 0xAD, 0xBE, 0xEF] -> "deadbeef"
            let tokenData = Data([0xDE, 0xAD, 0xBE, 0xEF])
            let formatted = tokenData.map { String(format: "%02.2hhx", $0) }.joined()
            XCTAssertEqual(formatted, "deadbeef")
        }

        func testEmptyTokenData() {
            // Stub: Empty Data -> empty string
            let tokenData = Data()
            let formatted = tokenData.map { String(format: "%02.2hhx", $0) }.joined()
            XCTAssertEqual(formatted, "")
        }
    }
    ```

    **4. Create `ZenSocialTests/NotificationPayloadTests.swift`:**

    Test stubs for deep-link URL extraction from notification payloads:

    ```swift
    import XCTest
    @testable import ZenSocial

    final class NotificationPayloadTests: XCTestCase {

        func testExtractInstagramURLFromPayload() {
            // Stub: userInfo["instagram_url"] = "https://www.instagram.com/p/ABC123/"
            // should parse to a valid URL
            let userInfo: [String: Any] = ["instagram_url": "https://www.instagram.com/p/ABC123/"]
            let urlString = userInfo["instagram_url"] as? String
            let url = urlString.flatMap { URL(string: $0) }
            XCTAssertNotNil(url)
            XCTAssertEqual(url?.host, "www.instagram.com")
        }

        func testMissingInstagramURLReturnsNil() {
            // Stub: userInfo without "instagram_url" key should not produce a URL
            let userInfo: [String: Any] = ["other_key": "value"]
            let urlString = userInfo["instagram_url"] as? String
            XCTAssertNil(urlString)
        }

        func testInvalidURLStringReturnsNil() {
            // Stub: Invalid URL string should not produce a URL
            let userInfo: [String: Any] = ["instagram_url": "not a url %%%"]
            let urlString = userInfo["instagram_url"] as? String
            let url = urlString.flatMap { URL(string: $0) }
            XCTAssertNil(url)
        }
    }
    ```

    NOTE: The APNsTokenTests already have passing assertions (they test a pure data transformation). NotificationManagerTests stubs intentionally fail -- they will be filled in by Plan 01 when NotificationManager is created. NotificationPayloadTests have passing assertions for the extraction logic but will be expanded when the actual payload handling code is implemented.
  </action>
  <verify>
    <automated>cd /Users/gavin/Documents/Projects/ZenSocial && xcodebuild -project ZenSocial.xcodeproj -scheme ZenSocial -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build-for-testing 2>&1 | tail -10</automated>
  </verify>
  <done>ZenSocialTests target exists in the Xcode project. Three test stub files compile. APNsTokenTests and NotificationPayloadTests have passing assertions for pure logic. NotificationManagerTests stubs fail intentionally (to be implemented alongside NotificationManager in Plan 01).</done>
</task>

</tasks>

<verification>
1. Test target exists: `grep -c "ZenSocialTests" ZenSocial.xcodeproj/project.pbxproj`
2. Test files exist: `ls ZenSocialTests/NotificationManagerTests.swift ZenSocialTests/APNsTokenTests.swift ZenSocialTests/NotificationPayloadTests.swift`
3. Tests compile: `xcodebuild -project ZenSocial.xcodeproj -scheme ZenSocial -destination 'platform=iOS Simulator,name=iPhone 16' build-for-testing`
</verification>

<success_criteria>
- ZenSocialTests/ directory exists with three test files
- XCTest target is registered in project.pbxproj
- Test stubs compile (build-for-testing succeeds)
- APNsTokenTests and NotificationPayloadTests pass; NotificationManagerTests stubs fail (expected -- RED phase of TDD)
</success_criteria>

<output>
After completion, create `.planning/phases/03-push-notifications/03-00-SUMMARY.md`
</output>
