---
phase: 03-push-notifications
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements:
  - PUSH-01
  - PUSH-02

must_haves:
  truths:
    - "User visually confirms native iOS notification banner appears when Instagram has new activity (foreground)"
    - "User confirms notification tap navigates to Instagram content in ZenSocial"
    - "User confirms Settings toggle controls notification delivery"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 notification flow: permission prompt after login, foreground banner delivery, notification tap deep-link, and Settings toggle.

Purpose: Confirm all user-facing behaviors work correctly on a real device or simulator before marking Phase 3 complete.
Output: Verified notification flow; phase ready for completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-push-notifications/03-CONTEXT.md
@.planning/phases/03-push-notifications/03-01-SUMMARY.md
@.planning/phases/03-push-notifications/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete notification flow</name>
  <files>none</files>
  <read_first>
    .planning/phases/03-push-notifications/03-01-SUMMARY.md,
    .planning/phases/03-push-notifications/03-02-SUMMARY.md
  </read_first>
  <action>
    Present the user with the following verification checklist. This is a human-verify checkpoint -- no code changes needed.

    What was built (Plans 01 + 02):
    - NotificationManager with UNUserNotificationCenter delegation
    - Permission flow: pre-prompt after Instagram login, then iOS system dialog
    - JS notification bridge detecting Instagram badge changes
    - Local notification scheduling (foreground banners)
    - BGAppRefreshTask registration for background delivery
    - Deep-link routing on notification tap (switches to Instagram tab, loads URL)
    - Settings toggle for enabling/disabling notifications

    Verification steps for the user:

    **Build and run on simulator (or device if available):**
    1. Clean build: Cmd+Shift+K then Cmd+R in Xcode

    **Permission flow (D-07, D-09):**
    2. Launch app fresh (delete app from simulator first for clean state)
    3. Navigate to Instagram tab
    4. Log into Instagram
    5. After login completes (feed loads), verify a "Stay in the loop" alert appears
    6. Tap "Enable Notifications" -- verify iOS system permission dialog appears
    7. Grant permission

    **Foreground notification (PUSH-01, D-10):**
    8. With app in foreground on Instagram tab, wait for notification badge detection
    9. If Instagram has notifications (activity badge visible), verify a native iOS banner appears at the top of the screen
    10. NOTE: If no real Instagram notifications exist, you can test by triggering a local notification manually via Xcode debug console or by having another account interact with the test account

    **Deep-link (D-11):**
    11. When a notification banner appears, tap it
    12. Verify the app switches to the Instagram tab (if not already there) and navigates to the activity page

    **Settings toggle (D-08):**
    13. Navigate to Settings (via home screen)
    14. Verify "Instagram Notifications" toggle is visible and ON
    15. Toggle it OFF
    16. Verify no new notifications appear (badge detection should be suppressed)
    17. Toggle it back ON

    **Denied state:**
    18. Go to iOS Settings > ZenSocial > Notifications and disable
    19. Return to ZenSocial Settings screen
    20. Verify "Open Settings to Enable" button appears instead of toggle
    21. Tap it -- verify it opens iOS Settings for ZenSocial
  </action>
  <verify>Human visual and interactive verification -- no automated command applicable for push notification testing on simulator/device.</verify>
  <acceptance_criteria>
    - Pre-prompt alert appears after Instagram login, not at app launch
    - iOS system permission dialog appears after user taps Enable in pre-prompt
    - Native banner notification appears in foreground when Instagram has new activity
    - Tapping notification navigates to Instagram content in ZenSocial
    - Settings toggle controls notification delivery
    - Denied state shows "Open Settings" button that links to iOS Settings
  </acceptance_criteria>
  <done>User has verified all notification behaviors per D-07 through D-11. Phase 3 is approved for completion.</done>
</task>

</tasks>

<verification>
All verification is human-driven for this plan. Push notification delivery cannot be meaningfully tested in automated CI without real APNs provisioning.
</verification>

<success_criteria>
- User confirms all notification behaviors work as specified in D-07 through D-11
- No visual or functional regressions in existing app features (dark theme, navigation, platform switching)
- Phase 3 ready for completion
</success_criteria>

<output>
After completion, create `.planning/phases/03-push-notifications/03-03-SUMMARY.md`
</output>
